// include/core/Enums.hpp
#pragma once

// Enumeration for different data structures used in the project
enum class DataStructureEnum {
    LIST,
    HEAP,
    GRAPH,
    CUSTOM,
    UNKNOWN
};

// Enumeration for different algorithms used in the project
enum class AlgorithmEnum {
    INSERTION_SORT,
    MERGE_SORT,
    HEAP_BUILD,
    HEAP_SELECTION,
    A_STAR,
    PRIMS,
    CUSTOM,
    UNKNOWN
};